% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uno_solve.R
\name{uno_solve}
\alias{uno_solve}
\title{Solve a nonlinear program with Uno}
\usage{
uno_solve(
  n,
  lb,
  ub,
  sense,
  obj,
  grad,
  m,
  cl,
  cu,
  cons,
  jac_rows,
  jac_cols,
  jac,
  hess_rows,
  hess_cols,
  hess,
  x0,
  preset,
  base_indexing,
  verbose,
  options = list(),
  lagrangian_sign = c("negative", "positive"),
  dual0 = NULL
)
}
\arguments{
\item{n}{number of variables.}

\item{lb, ub}{variable lower/upper bounds (length `n`; use `-Inf`/`Inf`).}

\item{sense}{`"minimize"` or `"maximize"`.}

\item{obj, grad}{objective `function(x)` and its gradient `function(x)`.}

\item{m}{number of constraints (0 for unconstrained).}

\item{cl, cu}{constraint lower/upper bounds (length `m`).}

\item{cons}{constraint `function(x)` returning a length-`m` vector.}

\item{jac_rows, jac_cols}{COO row/column indices of the Jacobian nonzeros.}

\item{jac}{Jacobian `function(x)` returning the nonzero values.}

\item{hess_rows, hess_cols}{COO indices of the lower-triangular Hessian.}

\item{hess}{Lagrangian Hessian `function(x, sigma, lambda)` returning the
lower-triangular nonzero values, or `NULL` (Uno then uses an L-BFGS
approximation, which the HiGHS subproblem solver cannot use).}

\item{x0}{initial primal iterate (length `n`).}

\item{preset}{Uno preset, e.g. `"filtersqp"` (SQP) or `"ipopt"` (interior
point, using MUMPS as the linear solver).}

\item{base_indexing}{0 for C-style or 1 for Fortran-style COO indices.}

\item{verbose}{if `FALSE`, suppress Uno's solution printout.}

\item{options}{a named list of Uno solver options applied AFTER the preset
(so they override it), e.g. `list(max_iterations = 200L, tolerance = 1e-8,
linear_solver = "MUMPS")`. Each value is coerced to the option's declared
Uno type; an unknown option name or an unacceptable value raises an error.}

\item{lagrangian_sign}{the Lagrangian multiplier sign convention the `hess`
callback uses: `"positive"` for \eqn{L = \sigma f + y^\top c} (the standard
convention used by IPOPT and the sparsediff oracle) or `"negative"` for
\eqn{L = \sigma f - y^\top c}. Defaults to `"negative"`, matching Uno's own
C-API default. **Must match the convention your `hess` returns**, otherwise
the Lagrangian Hessian's constraint terms get the wrong sign (invisible
when all constraints are linear, since their Hessian is zero).}

\item{dual0}{optional warm-start dual iterate, or `NULL` (Uno's default).}
}
\value{
a named list with the optimization/solution status (canonical
  character names, e.g. `"SUCCESS"`, `"FEASIBLE_KKT_POINT"`), objective,
  primal and dual solutions, KKT residuals, and per-callback evaluation
  counters.
}
\description{
Solve a nonlinear program with Uno
}
